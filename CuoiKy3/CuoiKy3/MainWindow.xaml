<Window x:Class="CuoiKy3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CuoiKy3"
        mc:Ignorable="d"
        Title="Quản lý học sinh" Height="600" Width="800">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="20 0 0 0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="400"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="5"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Red"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="Aqua"/>
            <Setter Property="Width" Value="55"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15" BorderBrush="Orange" BorderThickness="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   FontSize="30" FontWeight="Bold" HorizontalAlignment="Center"
                   Margin="10">QUẢN LÝ HỌC SINH</TextBlock>
        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">Mã học sinh:</Label>
        <Label Grid.Row="2" Grid.Column="0" VerticalAlignment="Center">Họ tên:</Label>
        <Label Grid.Row="3" Grid.Column="0" VerticalAlignment="Center">Ngày sinh:</Label>
        <Label Grid.Row="4" Grid.Column="0" VerticalAlignment="Center">Giới tính:</Label>
        <Label Grid.Row="5" Grid.Column="0" VerticalAlignment="Center">Con thương binh liệt sĩ:</Label>
        <Label Grid.Row="6" Grid.Column="0" VerticalAlignment="Center">Lớp:</Label>
        <TextBox x:Name="matxt" Grid.Row="1" Grid.Column="1"/>
        <TextBox x:Name="tentxt" Grid.Row="2" Grid.Column="1"/>
        <DatePicker x:Name="nsdate" Grid.Row="3" Grid.Column="1" Width="400" Height="30" Margin="5" HorizontalAlignment="Left"
                    VerticalAlignment="Center"/>
        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
            <RadioButton x:Name="radnam" IsChecked="True" GroupName="gt">Nam</RadioButton>
            <RadioButton x:Name="radnu" GroupName="gt">Nữ</RadioButton>
        </StackPanel>
        <CheckBox x:Name="checkCTBLS" Grid.Row="5" Grid.Column="1"/>
        <ComboBox x:Name="lopcbb" Grid.Row="6" Grid.Column="1" Width="400" Height="30" Margin="5" HorizontalAlignment="Left"/>
        <DataGrid x:Name="dgvTT" Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" AutoGenerateColumns="False"
                  Width="700" Height="200" HorizontalAlignment="Center" IsReadOnly="True" Margin="10"
                  SelectionChanged="selection_changed">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã học sinh" Binding="{Binding MaHs}"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding HoTen}" Width="120"/>
                <DataGridTextColumn Header="Ngày sinh" Binding="{Binding NgaySinh,StringFormat=dd/MM/yyyy}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Red"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Giới tính" Binding="{Binding GioiTinh}"/>
                <DataGridTextColumn Header="Con thương binh liệt sĩ" Binding="{Binding ConTbls}" Width="150"/>
                <DataGridTextColumn Header="Tên lớp" Binding="{Binding TenLop}" Width="120"/>
                <DataGridTextColumn Header="Tuổi" Binding="{Binding Tuoi}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnThem" Click="btnThem_Click">Thêm</Button>
            <Button x:Name="btnSua" Click="btnSua_Click">Sửa</Button>
            <Button x:Name="btnXoa" Click="btnXoa_Click">Xoá</Button>
            <Button x:Name="btnTim" Click="btnTim_Click">Tìm</Button>
            <Button x:Name="btnThongKe" Click="btnThongKe_Click">Thống kê</Button>
            <Button x:Name="btnXoaForm" Click="btnXoaForm_Click">Xoá form</Button>
            <Button x:Name="btnDong" Click="btnDong_Click">Đóng</Button>
        </StackPanel>


    </Grid>
</Window>
